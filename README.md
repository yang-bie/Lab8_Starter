# Lab8-Starter

Graceful degradation and service workers are related because a service worker can add higher-level features, like offline caching and faster repeat loads, while the site should still work when that technology is unavailable or fails. The page should be usable through normal network requests first, and the service worker should improve the experience without becoming the only way the app can load content.
